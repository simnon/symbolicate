# To have these files accessible from any folder in your terminal
echo "sudo is needed to move files into /user/local/bin"
sudo chmod +x symbolicate
sudo chmod +x symbolicateFrameworks
sudo mv symbolicate /usr/local/bin/symbolicate
sudo mv symbolicateFrameworks /usr/local/bin/symbolicateFrameworks
